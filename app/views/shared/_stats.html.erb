<% @user ||= current_user %>
<div class="stats">
    <strong id="following" class="stat">
      <% if @user.donated %>
        <span style="color: green;">Donated!</span>
      <% else %>
        <span style="color: red;">Hasn't donated yet.</span>
      <% end %>
    </strong>
</div>
